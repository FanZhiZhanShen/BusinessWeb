<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neuedu.dao.LogAspectjDao">
    <insert id="add" parameterType="com.neuedu.entity.LogAspectJ">
        insert aspectj(user_id,create_time,operation) VALUES(#{user_id},#{create_time},#{operation})
    </insert>
</mapper>